runtime:
  backend: gpu
  dtype: float32
  seed: 42

shape_buckets:
  residues: [128, 256, 512]
  msa_rows: 1024
  diffusion_steps: 200

paths:
  pytorch_checkpoint: ../boltz/.cache/boltz/boltz2_conf.ckpt
  processed_dir: ../boltz/benchmark_results/prep/boltz_results_prot_no_msa/processed
  output_dir: outputs
